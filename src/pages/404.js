import React from 'react';
import Layout from '../components/layout';

export default function PageNoteFound() {
  return (
    <Layout>
      <h3 style={{ paddingLeft: `3rem` }}>Sorry... This page doesn't exist.</h3>
    </Layout>
  );
}
